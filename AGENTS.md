# MDS 项目协作指南

## 项目背景与目标

- MDS 指 Market Data Study。
- 本项目研究如何仅根据股票的 `stock_id` 和源系统 `timestamp`，从一天的中国股票实时数据中恢复上游长期固定的股票分组。
- 最终目标是得到稳定的 `stock_id -> group_id` 映射，而不是只给单条记录或单次时间簇临时分组。
- 用户不能提供真实 CSV。进入验证阶段后，必须自行生成带真实标签的模拟数据；不要让方案依赖访问真实数据。
- 当前仍处于问题建模和方案讨论阶段。除非用户明确要求实现，否则不要编写算法、生成器或实验代码。

## 已确认的数据生成模型

把真实但不可见的一次上游推送记为隐藏事件 `p`：

- 股票全集存在多个长期固定的组；整个历史期间组关系不变。
- 每个隐藏推送事件只属于一个组 `G(p)`。
- 一次推送不保证包含组内全部股票，只推送该时刻实际发生变化的股票。因此该次出现的集合是 `S_p ⊆ G(p)`。
- 同一次推送绝不会混入其他组的股票。例如 `{1, 3}` 可以来自一组的一次推送；属于不同组的 `{1, 2}` 不可能来自同一个真实推送。
- 同一推送内各记录的时间戳接近但不完全相同，可表示为 `t = T_p + ε`，其中 `T_p` 是隐藏事件时间，`ε` 是较小抖动。
- 不同组的推送通常相隔明显更久，但时间尺度目前未知，组内与组间时间差分布允许少量重叠。
- 多个组偶尔会几乎同时推送。因此在观测数据里，不同组记录可能非常接近；例如两个组各推送约 2000 次时，可能约有 10 次相隔很近。这些记录仍属于不同的隐藏事件。
- 一天内同一股票会因成交等变化多次出现。
- 记录可能缺失；当前预期不会出现重复记录。
- 当前把 `timestamp` 视为源系统产生时间。
- 输入暂定为只包含一天、覆盖中国所有股票的 CSV。确切字段、精度、排序和数据规模尚未确认。

## 核心问题表述

CSV 中没有真实 `push_id`，所以不能直接观察隐藏事件。需要利用全天反复出现的共同到达模式，统计地恢复固定分组。

关键判断原则：

- 单次时间接近只能作为弱证据，不能证明两只股票同组。
- 同组关系应表现为跨多次事件重复出现、显著高于偶然碰撞水平的近时间同步。
- 不同组偶尔重叠是噪声，应由全局重复证据稀释，而不是被当成永久关系。
- 一次推送只含部分成员，因此不能要求同组所有股票总是一起出现，也不能要求组内每一对股票都有直接共同出现记录。
- 如果直接关系形成稳定证据链，例如 `1 <-> 3 <-> 5 <-> 8`，可以研究通过全局图结构识别间接同组关系。

## 当前值得继续研究的方向

以下是候选方向，不是已经选定的最终算法：

1. 为股票对构造经过更新活跃度校正的同步强度，而不是只计算原始近邻次数。
2. 研究两只股票之间的最近时间差分布。同组股票预期在零附近有重复尖峰，同时允许大量未共同出现的更新；不同组主要呈现背景碰撞，只有少量近零样本。
3. 考虑双向或一对一的时间匹配，避免高频股票的一条或多条更新人为放大配对计数。
4. 将股票视为节点、统计同步证据视为加权边，再研究图聚类或社区发现方法。
5. 不预先押注单一时间阈值；研究多时间尺度、背景窗口或数据驱动的阈值校准，并检查参数敏感性。
6. 利用分组全天固定这一事实做时间切片稳定性验证，例如上午/下午、分时段或交叉验证。
7. 除聚类结果外，还应输出证据强度或不确定性，以便处理低活跃、证据不足的股票。

任何后续方案都必须明确区分：

- 记录级的候选时间邻近；
- 隐藏推送事件；
- 最终固定的股票分组。

不要把这三层混为一谈。

## 当前不合适或不足的方案

- 不要把一次固定时间窗口内的所有股票直接永久归为同组。跨组推送偶尔重叠，会制造错误关系。
- 不要直接对全天记录做一次普通时间聚类（例如仅靠 DBSCAN）并把时间簇当作真实组。时间簇至多是候选隐藏事件，而且可能粘连多个组，也可能只覆盖真实组的一小部分。
- 不要因为两只股票有一次近时间记录就建立硬连接。
- 不要只用原始共同出现次数。股票活跃度不同，高频股票更容易与任何股票偶然碰撞。
- 不要假设每次推送包含组内全部股票。
- 不要在时间尺度尚未确定时把某个固定毫秒阈值写成业务事实。
- 不要仅凭模拟数据上的单个聚类指标宣布方案有效；还需要参数敏感性、时间切片稳定性、失败案例和可识别性分析。

## 可识别性边界

只看 `stock_id + timestamp` 时，真实分组不一定总能恢复。方案和报告必须正视以下必要条件：

- 同组股票之间需要有足够多的直接或间接共同推送证据。
- 每个真实组在统计关系图上至少应基本连通。
- 跨组偶然同步率需要显著低于组内同步信号。
- 一天的数据量必须足以区分稳定关系与随机碰撞。

例：真实组为 `{1, 3, 4}`，但 `1` 一天内始终单独出现，只有 `3` 和 `4` 共同出现，则时间戳数据无法证明 `1` 属于该组。这属于信息不足，而不是简单的算法失败。结果设计应允许“证据不足/未确定”，除非用户以后明确要求所有股票必须被强制分组。

## 尚待确认的问题

继续讨论时优先澄清，但不要阻塞可以安全推进的分析：

- 同一股票的两个真实推送是否可能在极短时间内连续发生，从而在组内时间窗口中出现两条记录？
- 每只股票是否保证恰好属于一个组，还是允许未分组或多重归属？
- “无重复”的精确定义：同一隐藏推送内不重复，还是 CSV 层面不存在完全重复行？
- CSV 的确切列名、时间精度、时区、排序方式和典型行数。
- 组数、组大小是否完全未知，以及是否存在可用的合理上下界。
- 不同组和不同股票的推送频率是否可能相差很大；低流动性股票一天内有多少有效证据。
- 同组时间抖动和组间间隔的大致量级与分布形状。
- 期望输出是否允许未分配股票、置信度、候选组或人工复核列表。
- 最终更重视精确率、召回率、完整覆盖，还是避免跨组误合并。

## 模拟与验证原则

只有在用户要求进入实现或实验阶段后才开始此部分。

模拟数据生成器至少应支持配置：

- 股票数、真实组数和不均衡组大小；
- 各组推送频率及不同股票活跃度；
- 每次推送只抽取组内部分发生变化的股票；
- 组内时间抖动；
- 跨组推送的常规间隔和低概率近时间重叠；
- 记录缺失且不生成非业务预期的重复；
- 低活跃股票、极小组、连续推送和其他边界案例。

模拟器必须保留真实 `group_id` 和隐藏 `push_id` 供评估，但待测算法只能读取真实 CSV 将拥有的字段，不能泄漏标签。评估候选可包括 pairwise precision/recall、ARI/NMI、覆盖率、低置信度数量、时间切片稳定性和跨参数稳定性；具体指标需先结合业务代价与用户确认。

## 仓库布局、Market Data 与交付规则

### 目录职责

- 本节中以 `./` 开头的路径均相对于“包含 `quant/` 子目录且存放规范 `discussion.md` 的工作区根目录”。当前环境中该目录是 `/Users/hanqingliu/Desktop/mds`；如果进程从 `./quant` 内启动，则下述 `./quant/mds/` 在仓库内对应 `./mds/`。
- 可提交代码的 Git 仓库是工作区根目录下的 `./quant`。
- 所有项目代码、Python 包、脚本及其测试必须实现于 `./quant/mds/` 下。不要在当前工作区根目录或 `./quant` 的其他业务目录新增实现代码，除非用户明确改变目录约定。
- `AGENTS.md`、`discussion.md`、讨论用样例和其他协作记录继续以当前工作区根目录中的版本为准。尤其是 `./discussion.md` 是规范讨论记录，不要改为维护 `./quant/mds/discussion.md`。
- `./quant/AGENTS.md` 是供进入 Git 仓库内部工作的指令镜像；修改根目录 `AGENTS.md` 时应保持该镜像同步，但根目录版本仍是当前工作区的规范来源。

### Market Data 输入与临时 Mock

- 用户可能要求实现处理 market data CSV 的 Python 脚本。用户会提供 CSV 表头或 schema，但不会提供真实数据；不要反复索要或假设能够访问真实数据。
- 实现前应把用户提供的字段名、类型、时间单位、时区、排序、缺失值和重复记录约定明确记录为已确认事实或显式假设。不要从列名静默推断高风险语义。
- 检查实现时，应根据已知 schema 和业务边界适度生成 mock 数据，覆盖正常数据、组内时间抖动、跨组偶发重叠、缺失、低活跃和用户已确认的其他边界情况。
- 本地检查使用的 mock CSV、临时输出、缓存和派生数据必须放在仓库外的临时目录，或放在明确被忽略且任务结束时清理的临时路径。不要将这些 mock 数据文件加入 Git 暂存区或最终提交。
- 如测试需要，可提交运行时生成 mock 数据的测试代码或生成器逻辑；生成出的数据文件本身不得提交。提交前必须检查暂存文件列表，确认没有 mock CSV、真实 market data、临时输出或敏感数据。

### 提交、Rebase 与推送

- 每次实现完成后，先运行与改动风险相称的测试和检查，并检查 `git diff`、`git diff --check`、暂存文件列表及工作树状态。
- 每次实现完成后都要创建清晰、范围明确的 commit，并推送到远端；除非用户明确要求暂停提交或推送。
- 每次向远端推送前，必须先在 `./quant` 中获取远端 `main` 最新状态，并将本地提交 rebase 到其上。标准顺序是：
  1. 保证待交付改动已经提交，工作树干净；
  2. 运行 `git fetch origin`；
  3. 运行 `git rebase origin/main`；
  4. 如远端更新导致实际代码变化或冲突解决，重新运行相关测试；
  5. 确认提交历史、暂存区和工作树正确后，运行 `git push origin main`。
- rebase 冲突时先理解双方意图并保护用户及远端改动；无法安全判断时停止并询问用户。不要通过丢弃改动来强行完成 rebase。
- 禁止使用 `git push --force` 或 `--force-with-lease`，除非用户针对该次操作明确授权。
- 推送后验证本地 `main` 与 `origin/main` 同步，并向用户报告 commit、测试结果和推送状态。

## `discussion.md` 的维护约定

- `discussion.md` 已在第一轮实质性讨论时创建。后续任务必须先阅读再继续。
- 如果该文件意外缺失，应重新创建，并用本文件中的既有共识初始化“当前状态”；不要凭空补充未讨论过的结论。
- 每次形成新共识、推翻假设、比较方案或得到实验结果后，在同一任务中更新 `discussion.md`，使新会话不依赖聊天历史也能接续。
- `discussion.md` 至少维护以下内容：
  - 当前阶段和最近更新时间；
  - 已确认事实与假设，二者必须分开；
  - 未决问题及优先级；
  - 候选方案、适用条件、风险和当前状态；
  - 已否决或暂不采用的方案及原因；
  - 决策记录及理由；
  - 已完成实验、结果、失败案例和可复现命令；
  - 下一步建议。
- 不要只保留最新观点而抹掉历史。维护一份简洁的当前摘要，同时保留重要决策和方案状态变化记录。
- 不要把猜测写成已确认事实；对每个关键结论注明其依据来自用户确认、数学推导还是模拟实验。

## 后续 Agent 的工作方式

开始任务时：

1. 完整阅读本文件。
2. 若 `discussion.md` 已存在，完整阅读它，并以其最新状态继续，不要从头重做已经完成的讨论或实验。
3. 检查当前目录中的代码、数据说明和未提交修改；保护用户已有工作。
4. 用简短中文说明当前理解、准备推进的具体问题以及所依赖的假设。

工作过程中：

- 默认使用中文与用户沟通；代码标识、数学符号和行业术语可使用英文。
- 先明确数据生成假设、可识别性和评价标准，再选择算法。
- 将业务事实、建模假设、启发式规则和实验结论清楚分层。
- 发现信息不足时，给出无法识别的具体反例，不要用任意算法强行制造确定答案。
- 对方案做比较时同时记录其优势、失败模式、计算复杂度和对未知时间阈值的依赖。
- 未经用户明确授权，不要把讨论自动扩展为实现。
- 用户要求实现后，应先做最小可复现模拟和基线，再逐步加入复杂噪声；每次实现都进行与风险相称的验证。
