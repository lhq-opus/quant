# MDS 讨论记录

## 当前状态

- 最近更新：2026-08-31
- 阶段：第一轮方案讨论；尚未选定算法，也未实现分组逻辑。
- 本轮产物：可人工检查的教学样例 CSV 与独立真值表。

## 已确认的业务事实

- 股票属于长期固定的上游分组，整个历史期间不变。
- 每次隐藏推送只属于一个组，但只包含当时发生变化的部分组内股票。
- 同一隐藏推送不会包含跨组股票。
- 同一次推送中的时间戳接近但不完全相同。
- 跨组推送大多数时候相隔较远，低概率会非常接近；组内与组间时间差分布可少量重叠。
- 一天内同一股票可被多次推送。
- 记录可能缺失，当前预期不会重复。
- 输入只有一天、覆盖中国股票的数据；真实 CSV 无法提供，后续需要模拟。
- `timestamp` 暂按源系统时间理解。

更完整的问题定义与长期约定见 `AGENTS.md`。

## 第一版教学样例

### 文件

- `data/sample_market_data.csv`：算法可见输入，仅有 `stock_id,timestamp`，共 89 条数据记录。
- `data/sample_ground_truth.csv`：仅用于人工检查和未来评估，不得作为算法输入。

### 真值分组

- `G_A = {1, 3, 4, 5, 8}`
- `G_B = {2, 6, 7, 9, 10}`
- `G_C = {11, 12, 13, 14, 15}`

### 仅用于该样例的建模假设

这些是假设，不是用户已经确认的业务事实：

- 每只股票在样例中恰好属于一个组。
- 典型组内抖动约为 0.1–1.8 ms。
- 每个隐藏推送只观察到 1–4 只组内股票；没有一次推送包含整个五只股票的组。
- `G_A` 在 `11:12:05.750` 附近的隐藏推送原计划包含 `{1,5,8}`，但模拟为 `5` 的记录缺失，输入只观察到 `{1,8}`。
- 样例没有构造“同一股票的两个不同推送在极短时间内连续发生”，因为这仍是未决业务问题。
- 样例规模只用于理解与排查算法行为，不足以代表全市场一天数据上的统计显著性。

### 刻意构造的困难案例

- `09:30:00.100` 附近 `G_A` 推送，约 4 ms 后 `G_B` 推送。若窗口过宽，会把两个组粘在一起。
- `10:03:10.100` 到 `10:03:10.107` 之间，`G_A`、`G_C`、`G_B` 三个组依次推送。单次时间聚类很容易产生跨组三组混合簇。
- `14:22:40.600` 附近 `G_A` 推送，约 4 ms 后 `G_C` 推送。
- 三个组都包含只出现一只股票的隐藏推送。单例事件本身不能提供同组关系证据。

### 已完成的数据检查

- 输入共有 89 行数据、15 只股票、3 个真实组；各组分别有 29、32、28 行记录。
- 所有时间戳均可解析且已升序排列。
- 没有完全重复行；输入中的股票集合与真值表完全一致。
- 按“相邻记录间隔超过阈值就切开”的朴素规则：
  - 阈值 2 ms：形成 33 个候选簇，其中已有 1 个跨组混合簇，共 5 行记录。
  - 阈值 5 ms：形成 30 个候选簇，其中 3 个跨组混合簇，共 20 行记录。
  - 阈值 10 ms：本样例结果与 5 ms 相同，仍有 3 个跨组混合簇、20 行记录。
- 2 ms 下也会误合并，是因为相邻记录会产生链式连接：`G_A` 的末条记录距离 `G_C` 的首条记录小于 2 ms。这个现象说明“单个簇的总跨度”可能远大于相邻间隔阈值。
- 使用一对一匹配计数检查股票对时：
  - 在 2 ms 窗口下，本教学样例 30 个真实组内股票对都有至少一次匹配，75 个跨组股票对中有 1 个出现匹配。
  - 在 5 ms 窗口下，30 个真实组内对仍都有匹配，但跨组有匹配的股票对增至 29 个。
  - 5 ms 下，部分跨组股票对的 Dice 同步分数达到约 0.36，而最弱真实组内对只有约 0.20。因此，仅用一次固定窗口内的 Dice 阈值无法同时保留全部弱组内关系并删除全部强偶然关系。
- 本样例刻意让每个真实组的组内配对证据较充分，不能据此假设真实数据中所有组内股票对都可直接观测到。

## 候选思路清单

所有状态均为本轮初步判断，尚未通过系统实验。

| 思路 | 当前判断 | 学习价值 / 优点 | 主要问题 |
|---|---|---|---|
| 固定时间窗口切簇后直接永久合组 | 不适合作为最终方案 | 最简单，可作为失败基线并帮助理解窗口敏感性 | 一次跨组近时间推送会产生错误合并；传递闭包可能造成链式污染 |
| 直接对记录时间做 DBSCAN | 不适合直接产生股票组；可作事件候选生成 | 能处理不规则时间间隔，比硬切整秒自然 | 聚的是记录时间，不是股票身份；重叠推送会粘连，部分推送又使簇不完整 |
| 股票对的原始近时间共同出现次数 | 弱基线 | 直观、容易解释和实现 | 高频股票天然碰撞更多；未校正活跃度，组间高频对可能得高分 |
| 时间分箱后计算二元序列相关性 | 可作基线 | 能把问题转成常见的相似度或相关性计算，易于向量化 | 分箱宽度敏感；大量共同的零会虚增相关性；相邻箱边界会拆散同一次推送 |
| 经活跃度归一化的配对计数 | 值得研究 | 比原始计数公平，可用 Dice/Jaccard/条件同步率等形式 | 一次推送只含部分成员时，分母定义很关键；仍依赖时间阈值 |
| 双向最近邻或一对一时间匹配 | 较有希望作为边证据 | 可避免一条高频记录重复匹配对方多条记录；保留精确时间差 | 两次真实推送过近时匹配会歧义；需要确定匹配窗口或使用多尺度版本 |
| 最近时间差分布 / 零附近尖峰检测 | 较有希望 | 同组预期在零附近反复出现尖峰，跨组主要是背景碰撞；不必假定每次共同出现 | 低活跃股票样本不足；市场活跃度随时段变化，需要局部背景模型 |
| 多尺度同步曲线 `C_ij(δ)` | 较有希望 | 不急于选唯一阈值；可比较计数随窗口增大的形状，同组通常在小尺度快速上升 | 股票对数量大，直接计算可能昂贵；曲线到单一边权的映射仍需设计 |
| 随机置换或强度模型估计偶然碰撞 | 统计上较稳健 | 能问“观察到的同步是否显著高于该股票活跃度下的随机预期” | 简单均匀 Poisson 假设不符合日内活跃度；置换必须保留午休、日内节奏和局部强度 |
| 加权图 + 社区发现 | 较有希望作为第二阶段 | 能利用间接证据链，不要求组内每一对股票直接共同出现 | 图边质量决定上限；错误强边会误合并，弱连接股票可能被错分或游离 |
| 相关聚类 / 带正负证据的图划分 | 值得研究 | 可以同时惩罚跨组强连接、保留不确定节点，目标更贴合固定分区 | “没有共同出现”通常不是负证据，因为推送只含部分成员；负边构造很危险 |
| 潜变量生成模型、EM 或贝叶斯模型 | 长期候选，不宜作为首个基线 | 可以显式建模隐藏推送、组归属、缺失与偶然重叠，并输出概率 | 可识别性、初始化、计算量和模型错设风险高；没有简单基线前很难判断收益 |
| 有监督或半监督分类 | 当前缺少条件 | 如果未来获得少量已知组，可大幅帮助阈值校准和不确定股票归属 | 当前没有真实标签；用模拟标签训练可能学到模拟器偏差 |
| 时间签名哈希、倒排索引或 LSH | 扩展规模时的工程候选 | 可避免对全市场所有股票对做完整两两比较 | 是候选对检索手段，不解决最终统计判定；近似检索可能漏掉弱关系 |

## 当前较合理的组合框架

这不是最终决策，而是下一轮最值得细化的主线：

1. 只在局部时间邻域生成候选股票对，避免全量 `O(N²)` 比较。
2. 对每个候选对做双向或一对一时间匹配，得到匹配时间差。
3. 同时考察多个窗口 `δ`，形成同步曲线，而不是立刻固定一个毫秒阈值。
4. 根据股票各自的局部更新强度估计随机碰撞基线，计算“超额同步”而不只是原始次数。
5. 将可靠的股票对证据组成加权图，再比较社区发现、相关聚类和保守的连通策略。
6. 对上午/下午或多个时间块重复建图；只信任跨时间块稳定的边和组。
7. 允许低证据股票处于未确定状态，并报告置信度与最接近的候选组。

一个值得研究的配对量是：

`C_ij(δ) = 在 |t_i - t_j| ≤ δ 下最多能形成的一对一匹配数`

但 `C_ij` 不能直接作为边权。至少还要与 `N_i`、`N_j` 以及随时段变化的偶然碰撞期望比较。简单的全日均匀近似可写为 `E[C_ij] ≈ 2δ·N_i·N_j/T`，但中国市场有开收盘效应、午休和活跃度波动，最终更可能需要分时段或局部强度版本。

## 尚未决定

- 是否允许算法输出未分组股票，还是必须强制完整分区。
- 每只股票是否业务上保证恰好属于一个组。
- 同一股票的两个真实推送能否在组内抖动尺度内连续发生。
- 实际时间精度与组内/组间尺度。
- 更应优先避免跨组误合并，还是优先提高组内覆盖率。
- 第一版算法应以可解释统计方法为主，还是允许较复杂的概率模型。

## 下一轮建议

- 先围绕“股票对的证据应该怎样定义”讨论，因为它是图方法和大多数聚类方法的共同基础。
- 用本样例手工比较几个窗口，观察固定窗口、原始计数、归一化计数和最近邻分布分别会犯什么错误。
- 达成指标定义后，再生成大规模可配置模拟数据；不要把这份 89 行教学样例当成算法性能测试集。
